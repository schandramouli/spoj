#include<stdio.h>

int * sort (int arr[], int n)
{
    for(i=0; i<n; i++)
    {
        for(i
    }
}

main()
{
    int n,k,s;
    int i;
    int sum=0, max,pos,no=0;
    scanf("%d%d%d",&n,&k,&s);
    int arr[1100];
    for(i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }

    int tot = k*s;
    arr = sort(arr,n);
    while(sum<tot)
    {
        

    }
    
    
    printf("%d",no);
    return 0;



}
